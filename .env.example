# Optional environment variables (set in PowerShell before launch).
# Defaults: loopback only, port 5000, database.db and uploads beside app.py.
PORT=5000
# SECRET_KEY=generate-a-long-random-value
# HUB_DATABASE=C:/path/to/database.db
# HUB_UPLOADS=C:/path/to/uploads
